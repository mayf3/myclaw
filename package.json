{
  "name": "myclaw-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "check": "find packages docs scripts -name '*.mjs' -print0 | xargs -0 -n1 node --check && node scripts/check-file-lines.mjs && node scripts/check-doc-phase-sync.mjs",
    "test": "node --test",
    "myclaw": "node packages/cli/src/index.mjs"
  },
  "engines": {
    "node": ">=20.11"
  }
}

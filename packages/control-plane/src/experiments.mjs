export function buildHumanExperimentsPayload() {
  const experiments = [
    experiment({
      id: "E0",
      milestone: "M0",
      title: "本地消息闭环",
      status: "ready",
      role: "本机使用者",
      whatToTest: "确认 CLI 能发送消息、写入 run/event，并能用 JSON 输出复核结果。",
      commands: ['npm run myclaw -- send --text "hello from human" --json'],
      successSignals: ["命令返回 ok envelope", "state 里新增 run", "Dashboard 最近 Runs 能看到这条消息"],
      nextUnlock: "E1 Dashboard 可视化检查",
    }),
    experiment({
      id: "E1",
      milestone: "M1",
      title: "Dashboard 可读性与路线图检查",
      status: "ready",
      role: "产品试用者",
      whatToTest: "打开 Dashboard，判断 3 分钟内能否看懂当前阶段、最近消息、参考完成度和下一步。",
      commands: [
        "npm run myclaw -- dashboard --port 4321 --openclaw-source $MYCLAW_OPENCLAW_SOURCE",
        "open http://127.0.0.1:4321",
        "curl -s http://127.0.0.1:4321/api/experiments",
      ],
      successSignals: ["Human Experiments 区块可见", "当前 Phase 显示 1.7", "每个实验都有命令和成功信号"],
      nextUnlock: "E1B Dashboard health 与 Gateway audit",
    }),
    experiment({
      id: "E1B",
      milestone: "M1",
      title: "Dashboard health 与 Gateway mutation audit",
      status: "ready",
      role: "本地运维观察者",
      whatToTest: "打开 Dashboard，确认顶部健康条能说明 HTML Center、state、Feishu adapter、mutation auth 状态，并且 Gateway 写操作会留下不含正文/token 的 audit 记录。",
      commands: [
        "MYCLAW_GATEWAY_TOKEN=dev-token npm run myclaw -- gateway --port 4322 --openclaw-source $MYCLAW_OPENCLAW_SOURCE",
        'curl -s http://127.0.0.1:4322/messages -H "content-type: application/json" -H "x-myclaw-token: dev-token" -d \'{"text":"audit smoke","reply":"ok"}\'',
        "curl -s http://127.0.0.1:4322/api/audit",
        "open http://127.0.0.1:4321",
      ],
      successSignals: [
        "Dashboard 顶部健康条显示 ok/warn/fail 汇总",
        "Gateway Mutation Audit 出现 gateway.message.receive",
        "audit JSON 不包含请求正文或 token 值",
      ],
      nextUnlock: "E1C Gateway event stream 与 scoped token",
    }),
    experiment({
      id: "E1C",
      milestone: "M1",
      title: "Gateway event stream 与 scoped token",
      status: "ready",
      role: "本地接入层验证者",
      whatToTest: "确认 Dashboard 能连接事件流，Gateway 非 loopback 场景下可用不同 token 分别授权写消息和读事件。",
      commands: [
        'MYCLAW_GATEWAY_SCOPED_TOKENS=\'[{"token":"message-token","scopes":["message:write"]},{"token":"events-token","scopes":["events:read"]}]\' npm run myclaw -- gateway --host 0.0.0.0 --port 4322 --openclaw-source $MYCLAW_OPENCLAW_SOURCE',
        'curl -i http://127.0.0.1:4322/messages -H "content-type: application/json" -H "x-myclaw-token: events-token" -d \'{"text":"scope denied"}\'',
        'curl -N http://127.0.0.1:4322/api/events/stream -H "x-myclaw-token: events-token"',
        "open http://127.0.0.1:4321",
      ],
      successSignals: [
        "events-token 写 `/messages` 返回 insufficient_scope",
        "events-token 读 `/api/events/stream` 返回 `event: snapshot`",
        "Dashboard 顶部显示 stream 已连接或 live",
        "SSE snapshot 不暴露 Feishu 正文、chat_id、sender_id 或 token",
      ],
      nextUnlock: "E2 Feishu outbound smoke",
    }),
    experiment({
      id: "E2",
      milestone: "M2",
      title: "Feishu custom-bot outbound",
      status: "needs_config",
      role: "飞书群机器人配置者",
      whatToTest: "用 Feishu/Lark custom bot webhook 验证 MyClaw 能把文本发到群里。",
      commands: [
        'export MYCLAW_FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/..."',
        'npm run myclaw -- send --channel feishu-webhook --webhook-url "$MYCLAW_FEISHU_WEBHOOK_URL" --text "hello from MyClaw" --json',
      ],
      successSignals: ["飞书群收到消息", "返回 result.code 为 0", "Dashboard 最新 run 显示 feishu-webhook"],
      nextUnlock: "E2A openduck credential presence smoke",
    }),
    experiment({
      id: "E2A",
      milestone: "M2",
      title: "Openduck Feishu credential presence smoke",
      status: "ready",
      role: "飞书测试群配置验证者",
      whatToTest: "从本机 openduck 备份配置导入 Feishu app 身份，只输出变量名与 credential 状态，不打印 secret。",
      commands: [
        "npm run import:openduck -- --json",
        "set -a; source .myclaw/openduck-feishu.env; set +a; npm run myclaw -- dashboard --port 4321 --openclaw-source $MYCLAW_OPENCLAW_SOURCE",
        "curl -s http://127.0.0.1:4321/api/feishu-adoption",
      ],
      successSignals: [
        "导入输出只有 writtenKeys/missingKeys，没有 secret 值",
        "Dashboard Feishu adapter 显示 app credentials ready、websocket runtime ready、app-token outbound ready",
        ".myclaw/ 仍是 git ignored",
      ],
      nextUnlock: "E2B Feishu websocket group reply",
    }),
    experiment({
      id: "E2B",
      milestone: "M2",
      title: "Feishu WebSocket 群消息自动回复",
      status: "ready",
      role: "飞书测试群使用者",
      whatToTest: "启动 MyClaw Feishu bot，在备份飞书群里发送文本消息，确认 bot 能通过长连接收到消息并用 app-token 回复。",
      commands: [
        "mkdir -p .myclaw && printf '%s\\n' '{\"allowedChatIds\":[\"oc_xxx\"]}' > .myclaw/feishu-policy.json",
        "set -a; source .myclaw/openduck-feishu.env; set +a; npm run myclaw -- feishu-bot --reply-prefix \"MyClaw 收到了\" --reply-mode direct",
        "在备份飞书群发送一条文本消息",
        "打开 Dashboard 查看最新 fb_* run",
      ],
      successSignals: [
        "终端显示 websocket ready",
        "无 policy 时 bot 会跳过消息，不会默认全群响应",
        "飞书群收到 MyClaw 收到了：<原消息>，不是话题回复",
        "Dashboard 最新 run 包含 feishu.bot.reply.completed，但 API 不暴露原文、chat_id、sender_id",
        "Feishu API 业务错误会记录为 feishu.bot.reply.failed，不会误报 completed",
      ],
      nextUnlock: "E2C Feishu hardening checks",
    }),
    experiment({
      id: "E2C",
      milestone: "M2",
      title: "Feishu hardening 与隐私读接口",
      status: "ready",
      role: "安全审阅者",
      whatToTest: "确认 Feishu bot 重启后不会重复回复同一事件，本地 policy 文件可限制群，Dashboard/API 对 Feishu run 脱敏。",
      commands: [
        "node --test packages/feishu-bot/test/feishu-bot.test.mjs packages/control-plane/test/http-routes.test.mjs",
        "mkdir -p .myclaw && printf '%s\\n' '{\"allowedChatIds\":[\"oc_xxx\"]}' > .myclaw/feishu-policy.json",
        "set -a; source .myclaw/openduck-feishu.env; set +a; npm run myclaw -- feishu-bot --reply-prefix \"MyClaw 收到了\" --reply-mode direct",
      ],
      successSignals: [
        "默认无 policy 时返回 ingress_policy_required，不发送回复",
        "重复 event 测试只发送一次回复",
        ".myclaw/feishu-policy.json 不会进入 git",
        "Feishu run API 中正文、chat_id、sender_id 显示为 [redacted]",
      ],
      nextUnlock: "E3 signed callback smoke",
    }),
    experiment({
      id: "E3",
      milestone: "M2",
      title: "Feishu callback 本地校验",
      status: "needs_config",
      role: "集成验证者",
      whatToTest: "先用 verify token 验证 callback challenge，再用测试套件覆盖 signed/encrypted fixture。",
      commands: [
        "MYCLAW_FEISHU_VERIFY_TOKEN=verify-token MYCLAW_FEISHU_ENCRYPT_KEY=encrypt-key npm run myclaw -- gateway --port 4322",
        'curl -s http://127.0.0.1:4322/feishu/events -H "content-type: application/json" -d \'{"token":"verify-token","challenge":"plain"}\'',
        "npm test -- packages/gateway/test/gateway.test.mjs",
      ],
      successSignals: ["本地 challenge 回显 plain", "签名错误 fixture 被拒绝", "encrypted challenge fixture 通过"],
      nextUnlock: "E4 migration review",
    }),
    experiment({
      id: "E4",
      milestone: "M3",
      title: "OpenClaw 迁移 review-only stage",
      status: "ready",
      role: "迁移审阅者",
      whatToTest: "确认一键迁移当前只做 plan/stage，不会直接 apply OpenClaw 运行时配置。",
      commands: [
        "npm run myclaw -- migrate openclaw --source $MYCLAW_OPENCLAW_SOURCE --json",
        "npm run myclaw -- migrate openclaw --source $MYCLAW_OPENCLAW_SOURCE --stage --json",
      ],
      successSignals: ["输出 destructive=false", "stage.forReviewOnly=true", "Dashboard stage summary 显示 missing/blocked"],
      nextUnlock: "E5 approval queue",
    }),
    experiment({
      id: "E5",
      milestone: "M4",
      title: "审批队列与迁移确认",
      status: "ready",
      role: "安全审阅者",
      whatToTest: "验证 OpenClaw stage 会生成 pending approval，并且用户能用 gateway token 明确 approve/reject。",
      commands: [
        "MYCLAW_GATEWAY_TOKEN=dev-token npm run myclaw -- gateway --port 4322 --openclaw-source $MYCLAW_OPENCLAW_SOURCE",
        'curl -s http://127.0.0.1:4322/api/openclaw-migration/stage -H "content-type: application/json" -H "x-myclaw-token: dev-token" -d "{}"',
        "curl -s http://127.0.0.1:4322/api/approvals",
        'curl -s http://127.0.0.1:4322/api/approvals/<approvalId>/decision -H "content-type: application/json" -H "x-myclaw-token: dev-token" -d \'{"decision":"rejected","reason":"human smoke"}\'',
      ],
      successSignals: ["pending approval 出现在 Dashboard", "decision 写入 approval record", "events timeline 出现 approval.decided"],
      nextUnlock: "E5B tool approval smoke",
    }),
    experiment({
      id: "E5B",
      milestone: "M4",
      title: "Tool approval smoke",
      status: "ready",
      role: "安全审阅者",
      whatToTest: "验证一个安全本地工具请求必须先进入审批队列，只有 approved 后才执行，rejected 不产生 tool-run。",
      commands: [
        "MYCLAW_GATEWAY_TOKEN=dev-token npm run myclaw -- gateway --port 4322 --openclaw-source $MYCLAW_OPENCLAW_SOURCE",
        'curl -s http://127.0.0.1:4322/api/tool-requests/smoke-note -H "content-type: application/json" -H "x-myclaw-token: dev-token" -d \'{"note":"human approval smoke"}\'',
        "curl -s http://127.0.0.1:4322/api/approvals",
        'curl -s http://127.0.0.1:4322/api/approvals/<approvalId>/decision -H "content-type: application/json" -H "x-myclaw-token: dev-token" -d \'{"decision":"approved","reason":"human smoke"}\'',
        "curl -s http://127.0.0.1:4322/api/tool-requests",
      ],
      successSignals: [
        "pending approval 的 subject.type 是 tool-action",
        "approved 后返回 tool.status=completed",
        "rejected 后返回 tool.status=rejected 且 result=null",
        "tool artifact 是相对路径 tool-runs/...，不会暴露本机绝对路径",
      ],
      nextUnlock: "E6A LLM reply smoke",
    }),
    experiment({
      id: "E6A",
      milestone: "M4",
      title: "LLM Reply Smoke",
      status: "ready",
      role: "真实智能回复测试者",
      whatToTest: "用 OpenAI Responses 验证 MyClaw 已能产生真实 LLM 文本回复；本阶段不开放工具调用。",
      commands: [
        'export OPENAI_API_KEY="sk-..."',
        'npm run myclaw -- ask --text "用三句话介绍 MyClaw 当前能做什么" --json',
        "set -a; source .myclaw/openduck-feishu.env; set +a; npm run myclaw -- feishu-bot --reply-provider llm --llm-privacy-ack --reply-mode direct",
      ],
      successSignals: [
        "`ask_*` run 写入 state",
        "JSON result 包含 provider=openai-responses、answer 和 toolCalls=[]",
        "缺少 OPENAI_API_KEY 时返回 llm_config_required，不假装回复",
        "Feishu LLM 模式必须显式使用 --reply-provider llm 和 --llm-privacy-ack",
      ],
      nextUnlock: "E6 agent tool loop",
    }),
    experiment({
      id: "E6",
      milestone: "M5",
      title: "单 Agent Runtime 实验",
      status: "planned",
      role: "长期使用者",
      whatToTest: "未来验证一次 agent 任务能拆解、让 LLM 选择工具、失败重试、请求人工确认，并被 Dashboard 追踪。",
      commands: ["后续开放：myclaw run --task ...", "后续开放：myclaw run --resume <runId>"],
      successSignals: ["任务有 step timeline", "tool call 有权限记录", "失败可重试或进入人工确认"],
      nextUnlock: "E8 Session provenance 实验",
    }),
    experiment({
      id: "E7",
      milestone: "M6",
      title: "工程约束红线检查",
      status: "ready",
      role: "协作开发者",
      whatToTest: "确认生成 HTML 不 stale，且每个目录最多 20 个文件、最多 4 层子目录、单文件最多 500 行都会被 npm run check 拦住。",
      commands: ["npm run check", "node scripts/check-generated-docs.mjs", "node scripts/check-file-lines.mjs"],
      successSignals: ["生成文档输出 up to date", "结构检查输出 20 files/dir", "结构检查输出 depth 4", "没有目录或文件超过红线"],
      nextUnlock: "下一阶段拆 Dashboard renderer 并接入 tool approval",
    }),
    experiment({
      id: "E8",
      milestone: "M7",
      title: "Session Search / Provenance 实验",
      status: "planned",
      role: "长期使用者",
      whatToTest: "未来验证 run、step、tool result 能按关键词检索，并且召回结果带来源，给单 Agent 和 Agent-to-Agent 复用。",
      commands: ["后续开放：myclaw search \"...\"", "后续开放：myclaw show <runId> --json"],
      successSignals: ["检索结果带 runId/stepId/messageId", "召回内容有来源和时间", "上下文注入有大小上限"],
      nextUnlock: "E9 Agent-to-Agent 协作实验",
    }),
    experiment({
      id: "E9",
      milestone: "M7",
      title: "Agent-to-Agent 协作实验",
      status: "planned",
      role: "协作任务测试者",
      whatToTest: "未来验证两个 agent 能围绕同一任务分工、交接上下文、互相审查结果，并留下可追踪记录。",
      commands: ["后续开放：myclaw run --task \"拆解并互审一个任务\" --agents 2 --json"],
      successSignals: ["每个 agent 有独立 run/step", "交接上下文有记录", "最终结果经过另一个 agent review"],
      nextUnlock: "E10 Long Memory / Search 长期记忆实验",
    }),
    experiment({
      id: "E10",
      milestone: "M7",
      title: "Long Memory / Search 长期记忆实验",
      status: "planned",
      role: "长期使用者",
      whatToTest: "未来验证 MyClaw 能保存长期事实、按来源检索、解释为什么召回，并支持遗忘或降权。",
      commands: ["后续开放：myclaw memory add ...", "后续开放：myclaw memory search \"...\"", "后续开放：myclaw memory delete <id>"],
      successSignals: ["召回结果带来源", "记忆能被 run 引用", "用户可删除或禁用一条记忆"],
      nextUnlock: "长期个性化和跨会话 agent",
    }),
  ];
  return {
    schemaVersion: 1,
    computed: false,
    source: "static-human-roadmap",
    currentPhase: "1.7",
    title: "Human Experiment Roadmap",
    goal: "把每个阶段改成用户可亲自验证的实验：先接入层和 gateway，再进入 agent、agent 协作和复杂记忆。",
    layerRoadmap: [
      layer("L0", "接入层", "人与外部系统的消息进入、发出和归一化。", "partial", ["E0", "E2", "E2A", "E2B", "E2C", "E3"]),
      layer("L1", "Gateway", "统一 HTTP 控制面、鉴权、状态查询、事件进入、SSE 事件流和 mutation audit。", "partial", ["E1", "E1B", "E1C", "E2A", "E2B", "E2C", "E3", "E5", "E5B"]),
      layer("L2", "Workflow 与审批", "迁移和后续工具调用必须先进入 review/approval。", "partial", ["E4", "E5", "E5B"]),
      layer("L3", "单 Agent Runtime", "一次任务能拆解、执行、失败重试、人工确认和验收；当前只有真实 LLM 单轮回复 smoke。", "partial", ["E6A", "E6"]),
      layer("L4", "Session Search / Provenance", "run、step、tool result 可检索，召回来源可解释。", "planned", ["E8"]),
      layer("L5", "Agent-to-Agent", "多个 agent 能分工、交接上下文、互相校验结果。", "planned", ["E9"]),
      layer("L6", "Long Memory / Search", "长期事实、来源解释、遗忘策略和跨会话召回。", "planned", ["E10"]),
    ],
    planningMap: [
      step("M0", "本地消息闭环", "E0", "立即可测"),
      step("M1", "Gateway 与 Dashboard", "E1/E1B/E1C", "立即可测"),
      step("M2", "Feishu/Lark 边界", "E2A/E2B/E2C/E2/E3", "WebSocket 群回复和 hardening 现在可测，webhook 配置后可测"),
      step("M3", "OpenClaw 迁移", "E4", "立即可测"),
      step("M4", "审批与安全", "E5/E5B/E6A", "立即可测"),
      step("M5", "Agent Runtime 与记忆", "E6", "LLM 回复已可测，工具循环后续开放"),
      step("M6", "工程约束与技术债", "E7", "立即可测"),
      step("M7", "分层交互测试路线", "E8/E9/E10", "路线已定义，功能后续开放"),
    ],
    experiments,
  };
}

function layer(id, label, purpose, status, experiments) {
  return { id, label, purpose, status, experiments };
}

function step(milestone, label, experimentId, userState) {
  return { milestone, label, experimentId, userState };
}

function experiment(input) {
  return input;
}
